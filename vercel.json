{
    "build": {
      "env": {
        "DATABASE_URL": "@your-environment-variable-name"
      },
      "command": [
        "yarn install --production",
        "prisma generate",
        "yarn build"
      ],
      "use": "@vercel/node"
    },
    "env": {
      "DATABASE_URL": "DATABASE_URL"
    }
  }
  